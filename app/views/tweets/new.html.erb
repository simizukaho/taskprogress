<h1>新規投稿ページ</h1>
<%= form_for @tweet do |f| %>

  <div class="field">
    <%= f.label :title, '課題内容' %>
    <%= f.text_field :title, :size => 50 %>
  </div>

  <%= f.submit "投稿する" %>
<% end %>

<%= link_to "Tweet一覧に戻る", tweets_path %>
